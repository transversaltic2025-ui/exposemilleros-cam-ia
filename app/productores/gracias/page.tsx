import Link from "next/link"; import { CheckCircle2 } from "lucide-react";
import { SiteShell } from "@/components/site-shell"; import { Card, CardContent } from "@/components/ui/card";
export default async function Gracias({ searchParams }: { searchParams: Promise<{ codigo?: string }> }) { const { codigo } = await searchParams; return <SiteShell><Card className="mx-auto max-w-2xl"><CardContent className="py-12 text-center"><CheckCircle2 className="mx-auto size-14 text-[var(--color-success)]"/><h1 className="expo-page-title mt-5">Iniciativa registrada correctamente</h1><p className="mt-4 text-[var(--color-muted)]">Su iniciativa productiva fue registrada correctamente. El equipo organizador revisará la información enviada.</p><p className="mt-6 font-bold">Código de la iniciativa: {codigo || "PROD-XXXX"}</p><div className="mt-8 flex flex-wrap justify-center gap-3"><Link className="rounded-xl bg-[var(--color-primary)] px-5 py-3 font-bold text-white" href="/productores/inscripcion">Registrar otra iniciativa</Link><Link className="rounded-xl border px-5 py-3 font-bold" href="/">Volver al inicio</Link></div></CardContent></Card></SiteShell>; }
