"use client";import {useState}from"react";import{useRouter}from"next/navigation";import{Button}from"@/components/ui/button";import{Input}from"@/components/ui/input";import{Label}from"@/components/ui/label";
export function EvaluadoraLogin(){const[documento,setDocumento]=useState("");const[error,setError]=useState("");const[busy,setBusy]=useState(false);const router=useRouter();async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError("");const r=await fetch("/api/productores/evaluadoras/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({documento})});const d=await r.json();setBusy(false);if(!r.ok)return setError(d.error);router.push(`/productores/evaluacion/${d.token}`)}return <form onSubmit={submit} className="space-y-4"><div><Label>Número de documento</Label><Input value={documento} onChange={e=>setDocumento(e.target.value)} required/></div>{error&&<p className="rounded-xl bg-red-50 p-3 text-sm text-red-700">{error}</p>}<Button disabled={busy}>{busy?"Validando...":"Ingresar"}</Button></form>}
