import Link from "next/link";import { BarChart3,Download } from "lucide-react";import { requireAdmin } from "@/lib/admin-auth";import { getProductoresDashboardData } from "@/lib/ai/productores-dashboard";import { ProductoresBatchControl } from "@/components/admin/productores/productores-batch-control";import { SiteShell } from "@/components/site-shell";import { MetricCard } from "@/components/metric-card";import { Card,CardContent,CardHeader,CardTitle } from "@/components/ui/card";import { buttonVariants } from "@/components/ui/button";
export const dynamic="force-dynamic";export default async function Page(){await requireAdmin();const {summary:s}=await getProductoresDashboardData();return <SiteShell><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="expo-eyebrow">Administración</p><h1 className="expo-page-title mt-2">Análisis masivo IA de productores campesinos</h1><p className="mt-3 text-sm text-[var(--color-muted)]">Procese las iniciativas por lotes y continúe de forma segura ante límites de OpenRouter.</p></div><div className="flex gap-2"><Link className={buttonVariants()} href="/admin/productores/tendencias/dashboard"><BarChart3/>Ver dashboard</Link><a className={buttonVariants({variant:"outline"})} href="/api/admin/productores/ai/dashboard/report/pdf"><Download/>Descargar reporte PDF</a></div></div><div className="mt-8 grid gap-4 md:grid-cols-4"><MetricCard label="Total iniciativas" value={s.total} detail="Registradas"/><MetricCard label="Análisis completados" value={s.analyzed} detail="Completados" accent="success"/><MetricCard label="Pendientes" value={s.pending} detail="Por analizar" accent="mint"/><MetricCard label="Con error" value={s.failed} detail="Reintentables" accent="secondary"/></div><Card className="mt-6"><CardHeader><CardTitle>Control del procesamiento</CardTitle></CardHeader><CardContent><ProductoresBatchControl total={s.total} analyzed={s.analyzed} pending={s.pending} failed={s.failed}/></CardContent></Card><Card className="mt-6"><CardContent className="py-5 text-sm"><b>Última fecha de análisis:</b> {s.lastAnalysis?new Date(s.lastAnalysis).toLocaleString("es-CO",{timeZone:"America/Bogota"}):"Sin análisis registrados"}</CardContent></Card></SiteShell>}
