import Link from "next/link";import { Download,Settings2 } from "lucide-react";import { requireAdmin } from "@/lib/admin-auth";import { getProductoresDashboardData } from "@/lib/ai/productores-dashboard";import { ProductoresDashboard } from "@/components/admin/productores/productores-dashboard";import { SiteShell } from "@/components/site-shell";import { buttonVariants } from "@/components/ui/button";
export const dynamic="force-dynamic";export default async function Page(){await requireAdmin();const data=await getProductoresDashboardData();return <SiteShell><div className="mb-6 flex flex-wrap items-end justify-between gap-4"><div><p className="expo-eyebrow">Analítica administrativa</p><h1 className="expo-page-title mt-2">Dashboard IA de productores campesinos</h1><p className="mt-3 text-sm text-[var(--color-muted)]">Lectura consolidada de madurez, potencial, acompañamiento e inclusión con evidencia explícita.</p></div><div className="flex gap-2"><Link className={buttonVariants({variant:"outline"})} href="/admin/productores/tendencias"><Settings2/>Gestionar análisis</Link><a className={buttonVariants()} href="/api/admin/productores/ai/dashboard/report/pdf"><Download/>Descargar reporte PDF</a></div></div><ProductoresDashboard data={data}/></SiteShell>}
