"use client";import{useState}from"react";import{useRouter}from"next/navigation";import{Button}from"@/components/ui/button";export function AnalyzeProductorButton({iniciativaId}:{iniciativaId:string}){const[busy,setBusy]=useState(false);const[error,setError]=useState("");const router=useRouter();async function run(){setBusy(true);setError("");const r=await fetch("/api/productores/ai/analyze",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({iniciativa_id:iniciativaId})});const d=await r.json();setBusy(false);if(!r.ok)return setError(d.error||"No fue posible generar el análisis.");router.refresh()}return <div><Button onClick={run} disabled={busy}>{busy?"Analizando...":"Generar análisis IA"}</Button>{error&&<p className="mt-2 text-sm text-red-600">{error}</p>}</div>}
