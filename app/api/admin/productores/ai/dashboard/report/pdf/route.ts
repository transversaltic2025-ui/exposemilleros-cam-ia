import { isAdminAuthenticated } from "@/lib/admin-auth";import { getProductoresDashboardData } from "@/lib/ai/productores-dashboard";import { buildProductoresDashboardPdf } from "@/lib/ai/productores-dashboard-report-pdf";export const runtime="nodejs";export async function GET(){if(!(await isAdminAuthenticated()))return Response.json({success:false,error:"No autorizado"},{status:401});const pdf=buildProductoresDashboardPdf(await getProductoresDashboardData());return new Response(new Uint8Array(pdf),{headers:{"Content-Type":"application/pdf","Content-Disposition":"attachment; filename=\"reporte-productores-campesinos-ia.pdf\"","Cache-Control":"no-store"}});}
