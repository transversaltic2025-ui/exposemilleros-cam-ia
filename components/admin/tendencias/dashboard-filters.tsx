import { RotateCcw } from "lucide-react";
import { Button } from "@/components/ui/button";
export type TrendFilters={line:string;seedbed:string;trend:string;status:string;gender:string};
export function DashboardFilters({value,onChange,options}:{value:TrendFilters;onChange:(value:TrendFilters)=>void;options:{lineas:string[];semilleros:string[];nivelesTendencia:string[];estadosAnalisis:string[]}}){const field=(key:keyof TrendFilters,label:string,values:string[])=><label className="grid gap-1.5 text-xs font-bold text-slate-600"><span>{label}</span><select className="h-10 min-w-0 rounded-xl border border-slate-200 bg-white px-3 text-sm font-medium text-slate-900 outline-none focus:border-violet-500 focus:ring-2 focus:ring-violet-100" value={value[key]} onChange={(event)=>onChange({...value,[key]:event.target.value})}><option value="Todos">Todos</option>{values.map((item)=><option key={item} value={item}>{item}</option>)}</select></label>;return <div className="rounded-2xl border border-slate-200 bg-white p-4 shadow-[0_8px_30px_rgba(15,23,42,0.05)]"><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-[repeat(5,minmax(0,1fr))_auto]">{field("line","Línea temática",options.lineas)}{field("seedbed","Semillero",options.semilleros)}{field("trend","Nivel de tendencia",options.nivelesTendencia)}{field("status","Estado de análisis",options.estadosAnalisis)}{field("gender","Enfoque de género",["Explícito","Sin información explícita"])}<Button className="self-end" variant="outline" onClick={()=>onChange({line:"Todos",seedbed:"Todos",trend:"Todos",status:"Todos",gender:"Todos"})}><RotateCcw/>Limpiar</Button></div></div>}
